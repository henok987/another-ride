# Mongo
MONGO_URI=mongodb://localhost:27017/booking

# Ports
BOOKING_PORT=4001
DISCOVERY_PORT=4002
USERPROXY_PORT=4003

# Auth / JWT
JWT_SECRET=change_me_for_hmac
AUTH_JWKS_URL=
AUTH_AUDIENCE=
AUTH_ISSUER=
AUTH_DEBUG=0

# External User Service (Auth/User microservice)
# Example using base and templated endpoints
AUTH_BASE_URL=https://auth.example.com/api
PASSENGER_LOOKUP_URL_TEMPLATE=https://auth.example.com/api/passengers/{id}
DRIVER_LOOKUP_URL_TEMPLATE=https://auth.example.com/api/drivers/{id}

# If the external service requires a static bearer for service-to-service calls
AUTH_SERVICE_BEARER=

